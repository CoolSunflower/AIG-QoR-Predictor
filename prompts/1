My goal is to create a QoR (area) prediction model, given a circuit design (represented as an AIG in ABC) and a recipe.

Input: 
- dataset.csv: columns: design_name, step1, and1, step2, and2, ..., step15, and15
- designs/*.bench: for each design_name in the dataset.csv a .bench file containing the structure in the format of ABC

Overview of proposed model:
- The circuit will be encoded with rich node wise encoding caculated using GNNs, either as a node-wise embedding matrix or as a global embedding vector
- The recipe will be encoded with rich node wise encoding calculated using GNNs, as a matrix 
- These two encodings will be used to predict step by step and gate count in the circuit

Output:
- Trained GNN model for circuit encoding
- Trained GNN model for recipe encoding
- Trained Transformer model for step wise prediction
- Easy to use inference function

Steps of implementation:
- Before doing anything I will first find initial encoding of every design, which will made richer with a varying GNN in every minibatch
- Network architecture: takes in a design name and a recipe as an array of strings
- The training function will calculate step by step area prediction and compute loss from and values in the dataset
- The network forward pass will first find node wise embeddings of the given design_name (which will be constant in a mini batch) and then update them using Graph Neural Network
- The network will then use node wise embedding to either construct a global encoding for the design or to use the variable column-size node wise embedding matrix (I prefer this second approach but I am not sure how it will work with the later defined step wise predictions so can you please expand on this also)
- The network will then create a Recipe Embedding matrix via a GNN to create a matrix of size (embeddings_dim, total_recipe_steps)
- In a minibatch, it will first identify all the different designs and recipes and do the above encoding for all
- Then it will use a transformer model to predict a scalar value at each recipe step
- Then it will use a LSTM model to predict a scalar value at each recipe step
- The final prediction will be an average of the two predictions
- It will then calculate loss with given and values for each entry in the minibacth and update the weights
- Non convex optimisation so use scheduler and clipping


Additional Notes on embedding calculation:

1) Circuit Encoding
    Input design.bench file architecture:
        # Benchmark "bc0_orig" written by ABC on Sat Oct 23 17:57:02 2021
        INPUT(v0)
        INPUT(v1)
        INPUT(v2)
        ...
        INPUT(v24)
        INPUT(v25)
        OUTPUT(v26.0)
        OUTPUT(v26.1)
        OUTPUT(v26.2)
        ...
        OUTPUT(v26.8)
        OUTPUT(v26.9)
        OUTPUT(v26.10)
        new_n38_    = NOT(v0)
        new_n39_    = NOT(v1)
        new_n40_    = NOT(v2)
        ...
        new_n58_    = AND(v14, new_n53_)
        ...
        v26.10      = NOT(new_n2826_)

    Treat NOT as a type of edge in the initial graph that you construct.

    Initial node wise embedding calculation:
        - Normalised depth from output node
        - Normalised height from input node
        - Normalised Fan-out count
        - One hot to represent type of node (Input, Output, And)
        - One hot to represent type of inputs (Both normal, One Not One Normal, Both Not)
        - Normalised Input receptive field (Percentage of input nodes that impact this node)
        - Output impact field (Percentage of output nodes that are impacted by this node)

    Initial edge encoding:
        - type of edge (NOT or normal)
        
    You need to first calculate the embeddings for all the designs and save them as the calculation is computationaly hard, and then use them in the model for GNN

    Graph Structure:
        - The graph that is made from the above, save node wise embedding and graph in pytorch geometric data file which will be used in training

    GNN Architecture for updating embeddings:
        - Come up with this advanced stuff and a domain inspired model

    Global circuit encoding calculation: (if needed (I will provide later) based on which will be easier to work with a global embedding vector or a node-wise embedding matrix)

2) Recipe Encoding
Note that any of step{i} belongs to the following list of strings: COMMANDS = ["rewrite -z", "rewrite -l", "rewrite", "balance", "resub", "refactor", "resub -z", "refactor -z"].

    Initial Recipe encoding:
        - One hot encoding from the above list
        - Normalised position from start
    
    No edge encoding

    Recipe Graph Structure:
        - First connect operations sequentially
        - Connect operations of same type (irrespective of additional flags)
        - Connect operations of zero cost (-z)

Now analyse all this and provide me a detailed mathematical and theoretical formulation of the best strategy for this task.
 